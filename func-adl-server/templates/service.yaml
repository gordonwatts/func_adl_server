apiVersion: v1
kind: Service
metadata:
  name: {{ template "func-adl-server.fullname" . }}
  labels:
    app: {{ template "func-adl-server.name" . }}
    chart: {{ template "func-adl-server.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "func-adl-server.name" . }}
    release: {{ .Release.Name }}
