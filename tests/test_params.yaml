# Boot up a default configuration to run the tests.
# Use a custom port in case it is running with something else on the same machine!
external_interface:
  xrootd_port: 31001
  results_http_port: 31002
  web_port: 31000
